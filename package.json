{
  "name": "infinite-wiki",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "android-build": "vite build && npx cap sync android",
    "start-local": "npm run build && cp sw.js dist/ && cp icon-*.png dist/ && cp manifest.json dist/ && npx serve -s dist -l 3000"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/core": "^7.4.3",
    "@google/genai": "^1.7.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^7.4.3",
    "@types/node": "^22.14.0",
    "pm2": "^6.0.11",
    "serve": "^14.2.5",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
